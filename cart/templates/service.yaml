apiVersion: v1
kind: Service
metadata:
  name: {{- include "cart.name" }}
  namespace: {{ .Values.namespace }}
  labels: 
    {{- include "cart.labels" . | nindent4 }}
spec:
  selector:
    {{- include "cart.labels" . | nindent4 }}
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080